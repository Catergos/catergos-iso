sudo: required

services:
  - docker

arch:
  packages:
  - archiso
  mount:
  - ~/.pkg-cache:/var/cache/pacman/pkg
  before_install:
  - "mkdir catergos"
  - "mkdir /run/shm"
  script:
  - "cp -r /usr/share/archiso/configs/releng/* catergos"
  - "ls catergos"
  - "sudo catergos/build.sh -v"
  - "ls catergos"
  
script:
- "curl -s https://raw.githubusercontent.com/php007/arch-travis/master/arch-travis.sh | sudo bash"
